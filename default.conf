server {
    listen 51980; # If https is not enabled, this gets sed to 80
    # listen [::]:51980;

    client_max_body_size 4096m;

    location / {
        rewrite ^/(.*)$ /ui/$1 last;
    }

    include includes/*.conf;
}
